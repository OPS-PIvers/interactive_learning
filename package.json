{
  "name": "interactive-training-module-creator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:server": "tsc src/server/Code.ts --outDir dist --target ES2015 --module ES2015 --lib ES2015",
    "build:client": "vite build",
    "prepare-gas": "node scripts/prepare-gas-deployment.js",
    "copy-assets": "cp appsscript.json dist/ && cp .clasp.json dist/"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
